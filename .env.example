# Required only for live evaluation. Replay, dashboard, and visualization do
# not call the provider. Export this value in your shell or load it with your
# preferred secret manager; setup never copies it into roborsi.yaml.
OPENAI_API_KEY=
