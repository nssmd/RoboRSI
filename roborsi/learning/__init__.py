"""roborsi.learning — automated self-improvement primitives.

Modules:
  auto_apply          — git-apply a proposal, verify via bench, commit or revert
  base_skill_extractor — extract reusable base skills from successful traces

Used by ``roborsi selfevo run`` (Claim 1: self-evolution) and (later)
``roborsi flywheel run`` (Claim 2a: skill-code flywheel).
"""
